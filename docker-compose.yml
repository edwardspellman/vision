version: '3.8'

services:
  uchat:
    build: .
    container_name: uchat-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    volumes:
      - uchat-uploads:/app/server/uploads

volumes:
  uchat-uploads:
